/**
 * agentkit-ui / foundation — domain-blind vocabulary + pure helpers.
 *
 * severity: ranked-band + bucket-fold engine (no styling).
 */
export {
  SEVERITIES_BY_RANK,
  SEVERITY_RANK,
  rankFor,
  BUCKETS,
  BUCKET_FROM_RAW,
  bucketFor,
} from "./severity";
export type { RawSeverity, Bucket } from "./severity";
