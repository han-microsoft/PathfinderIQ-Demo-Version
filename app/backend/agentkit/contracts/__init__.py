"""agentkit.contracts — wire/boundary contracts shared across consumers.

Modules:
    - ``envelope`` — canonical tool error/degraded envelope helpers.
    - ``models`` — generic conversation/wire pydantic types.
    - ``errors`` — generic structured error taxonomy.
"""
