"""Package entry point for graph_data automation scripts.

Module role:
    Makes the ``scripts`` directory importable as a normal Python package so
    new package-building, upload, and publish modules can be executed via
    ``python -m scripts.<module>`` and imported from tests.
"""
